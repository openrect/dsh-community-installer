{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Access required by the local DSH controller UI.",
  "windows": ["main", "tray", "logs", "prompt"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-minimize",
    "core:window:allow-start-dragging"
  ]
}
